import styled from 'styled-components'

export const Table = styled('table')(() => ({
  display: 'table',
  width: '100%',
  borderCollapse: 'separate',
  borderSpacing: '0px',
  overflow: 'auto'
}))
